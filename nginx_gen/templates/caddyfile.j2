# Generated by nginx-config-gen (Caddyfile)
{% for vhost in vhosts %}
{{ vhost.server_name }} {
{% if vhost.gzip_enabled %}
    encode zstd gzip
{% endif %}

{% for loc in vhost.locations %}
    handle {{ loc.path }}* {
        reverse_proxy {{ loc.proxy_pass | replace("http://", "") | replace("https://", "") }}
    }
{% endfor %}
}
{% endfor %}
